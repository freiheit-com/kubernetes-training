apiVersion: v1
kind: Pod
metadata:
  labels:
    app: health
    version: v1
    name: health
  name: health-v1
spec:
      containers:
        - image: gcr.io/fdc-test-statistic/health:1
          env:
            - name: SLEEP
              value:  "0"
          name: health
          ports:
            - containerPort: 8000
              protocol: TCP
